#endpoint: ${env:PRISMA_ENDPOINT}
endpoint: https://us1.prisma.sh/leen-kim/backend/dev
datamodel: datamodel.graphql
# secret: ${env:PRISMA_SECRET} UNCOMMENT BEFORE PRODUCTION!!
hooks:
  post-deploy:
    - graphql get-schema -p prisma
